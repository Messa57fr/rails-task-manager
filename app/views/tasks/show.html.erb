<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<% message = "The task is completed" if @task.completed == true %>
<% message = "The task is NOT completed" if @task.completed == false %>
<p><%= message %></p>

<%= link_to "See all tasks", tasks_path %>
